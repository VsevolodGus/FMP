<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:lang="clr-namespace:Bioss.Ultrasound.Resources.Localization"
             x:Class="Bioss.Ultrasound.UI.Pages.MenuPage"
             Title="{x:Static lang:AppStrings.Menu_Title}">

    <CollectionView 
        SelectionMode="Single"
        ItemsSource="{Binding Settings}"
        SelectedItem="{Binding SelectedSetting, Mode=TwoWay}">

        <CollectionView.ItemTemplate>
            <DataTemplate>
                <StackLayout Padding="16, 12" Spacing="2">
                    <Label Text="{Binding Name}"/>
                    <Label Grid.Row="1" Text="{Binding Description}" FontSize="Caption"/>
                </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>

    </CollectionView>
    
</ContentPage>
<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:xct="http://xamarin.com/schemas/2020/toolkit"
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core"
             xmlns:lang="clr-namespace:Bioss.Ultrasound.Resources.Localization"
             x:Class="Bioss.Ultrasound.UI.Pages.StartupPage"
             ios:Page.UseSafeArea="True">

    <ContentPage.Behaviors>
        <xct:EventToCommandBehavior
            EventName="Appearing"
            Command="{Binding AppearingCommand}"/>
    </ContentPage.Behaviors>


    <StackLayout Padding="16">
        <Label Text="{x:Static lang:AppStrings.Startup_PermissionsMessage}"
               VerticalTextAlignment="Center"
               HorizontalTextAlignment="Center"
               VerticalOptions="CenterAndExpand"  />

        <Button Text="{x:Static lang:AppStrings.Startup_RequestPermissions}"
                Command="{Binding RequestBluetoothPermissionCommand}"/>
    </StackLayout>

</ContentPage>